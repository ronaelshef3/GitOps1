apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quakewatch-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ronaelshef3/gitops1'
    targetRevision: master
    path: APP-MANIFAST # התיקייה שבה יושב ה-Helm Chart שלך
    helm:
      valueFiles:
        - values.yaml # שימוש בערכים ששלחת לי קודם
  destination:
    server: https://kubernetes.default.svc
    namespace: default # האפליקציה תרוץ ב-Namespace הרגיל
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true # עוזר לעקוף שגיאות Schema